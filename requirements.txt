# Main app (rag_sys venv) - FastAPI, retrieval, generation, guardrails, Phase 2 LangGraph.
# Does NOT include ragas/langchain-community/langchain-google-genai -- those are
# only needed for the eval harness, which runs in a separate environment
# (ragas_venv, Python 3.12) because of a real langchain-core version conflict:
# ragas==0.2.15 needs langchain-core<1.0, langgraph needs langchain-core>=1.2.
# See requirements-ragas.txt for that environment's pins.

langchain-text-splitters>=0.3.0
langchain-core>=1.2.0
langgraph>=1.0.0
rank_bm25>=0.2.2
sentence-transformers>=3.0.0
numpy>=1.26.0
fastapi>=0.115.0
uvicorn>=0.30.0
requests>=2.32.0
pydantic>=2.8.0
qdrant-client>=1.12.0
redis>=5.0.0
google-genai>=1.0.0
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0
spacy>=3.7.0
diskcache>=5.6.0
python-dotenv>=1.0.0
